const fs = require('fs'); const path = require('path'); const files = fs.readdirSync(__dirname).filter(f => f.endsWith('.txt')); for(const f of files) { const text = fs.readFileSync(path.join(__dirname, f), 'utf8'); const m = text.match(/"manufacturerName"\s*:\s*"([^"]+)"/); const p = text.match(/"productId"\s*:\s*"([^"]+)"/); console.log(f + ': ' + (m?m[1]:'none') + ' - ' + (p?p[1]:'none')); }
