# .npmrc - Homey App publish config
# Note: Do NOT use omit=optional here — the homey SDK package depends on sharp
# (optional native binaries) and omitting optionals breaks npx homey app validate.
# Archive size is managed via .homeyignore instead.
