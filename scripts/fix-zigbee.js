const fs = require("fs"); const content = fs.readFileSync("drivers/zigbee/driver.compose.json", "utf8"); const fixed = content.replace(/"productId":\s*\[([^\]]+)\]/, `"productId": [$1],\n    "endpoints": {\n      "1": {\n        "clusters": [0, 6]\n      }\n    }`); fs.writeFileSync("drivers/zigbee/driver.compose.json", fixed); console.log("✅ Driver zigbee corrigé avec succès !");
