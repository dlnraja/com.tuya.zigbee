#!/usr/bin/env node
'use strict';

// !/usr/bin/env node/** * Homey App Test Tool * Inspiré de https://tools.developer.homey.app/ */const { execSync } = require('child_process');const path = require('path');class HomeyTestTool { constructor() { this.projectRoot = process.cwd(); } async test() { console.log('🧪 Tests Homey App...'); try { // Test d'installation execSync('npx homey app install', { cwd: this.projectRoot, stdio: 'inherit' }); console.log('✅ Tests réussis'); } catch (error) { console.error('❌ Erreur de test:', error.message); } }}const tool = new HomeyTestTool();tool.test().catch(console.error);