// !/usr/bin/env node/** * Homey App Validation Tool * Inspiré de https://tools.developer.homey.app/ */const { execSync } = require('child_process');const path = require('path');class HomeyValidationTool { constructor() { this.projectRoot = process.cwd(); } async validate() { console.log('🔍 Validation Homey App...'); try { // Validation debug execSync('npx homey app validate --level debug', { cwd: this.projectRoot, stdio: 'inherit' }); // Validation publish execSync('npx homey app validate --level publish', { cwd: this.projectRoot, stdio: 'inherit' }); console.log('✅ Validation réussie'); } catch (error) { console.error('❌ Erreur de validation:', error.message); } }}const tool = new HomeyValidationTool();tool.validate().catch(console.error);