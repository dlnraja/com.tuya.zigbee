name: Buildon push branches main, master pull_request branches main, masterjobs build runs-on ubuntu-latest steps - name Checkout repository uses actionscheckoutv4 - name Setup Node.js uses actionssetup-nodev4 with node-version 18 cache npm - name Install dependencies run npm ci - name Build application run npm run build - name Upload build artifacts uses actionsupload-artifactv4 with name build-artifacts path dist retention-days 30