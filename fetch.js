const { execSync } = require('child_process'); const ids = [360, 340, 337, 333, 329]; ids.forEach(id => { try { const out = execSync('gh issue view ' + id + ' --json title,body,number', { env: { ...process.env, GITHUB_TOKEN: '' } }); const data = JSON.parse(out); console.log('\n\n=== ISSUE ' + data.number + ': ' + data.title + ' ===\n'); console.log(data.body.substring(0, 1500)); } catch (e) { console.error('Error on', id, e.message); } });
