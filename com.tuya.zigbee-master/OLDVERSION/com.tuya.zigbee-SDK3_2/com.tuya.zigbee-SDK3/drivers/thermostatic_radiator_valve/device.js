'use strict'; const Homey = require('homey'); const { ZigBeeDevice } = require('homey-zigbeedriver'); const { Cluster, debug, CLUSTER, TimeCluster } = require('zigbee-clusters'); const TuyaSpecificCluster = require('../../lib/TuyaSpecificCluster') const TuyaSpecificClusterDevice = require('../../lib/TuyaSpecificClusterDevice'); const { getDataValue, parseSchedule, marshalSchedule, THERMOSTAT_DATA_POINTS } = require("./helpers"); Cluster.addCluster(TuyaSpecificCluster); /** * Sources: * * - PR with simple impl.: https: 
