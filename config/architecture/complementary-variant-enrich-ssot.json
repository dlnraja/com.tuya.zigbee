{
  "_meta": {
    "id": "P2520",
    "title": "Complementary variant enrich — never degrade",
    "updated": "2026-09-21",
    "classification": "BOTH",
    "human": "docs/rules/COMPLEMENTARY_VARIANT_ENRICH.md",
    "architecture": "docs/architecture/COMPLEMENTARY_ENRICHMENT.md",
    "gate": "npm run check:p2520",
    "mergeHelper": "lib/enrichment/ComplementaryMerge.js",
    "completenessNote": "SSOT docs enrich = UNION append (P2645 sleepy pairing + battery + pairing discovery) — never wipe prior patches"
  },
  "mandate": {
    "always": true,
    "everyPrompt": true,
    "everyEnrichScript": true,
    "everyCron": true
  },
  "vision": {
    "enrichmentsAre": [
      "variants",
      "additions",
      "parallel alternatives",
      "complementary layers"
    ],
    "enrichmentsAreNot": [
      "replacements",
      "wipes",
      "capability shrinks",
      "mfr/pid drops",
      "settings array replaces",
      "invented pids as hard locks"
    ]
  },
  "operations": {
    "manufacturerName": "union_case_aware",
    "productId": "union_exact_case_aware",
    "capabilities": "union_append_only",
    "settings": "append_by_id_never_wipe",
    "clusters": "union_numeric",
    "flowCards": "append_by_id_never_delete",
    "dpMaps": "merge_key_preserve_existing",
    "registry": "append_cases_never_drop_sacred"
  },
  "forbidden": [
    "compose.zigbee.manufacturerName = [onlyNew]",
    "compose.capabilities = [shrunk]",
    "compose.settings = [] then rewrite",
    "delete mfr because same brand elsewhere",
    "move couple by deleting from canonical then invent elsewhere",
    "overwrite curated users.*.devices",
    "force EF00 onto zcl_only sacred"
  ],
  "allowedRemovals": [
    "forbidden-placement rollback (misattribution registry)",
    "synthetic hybrid placeholders at prepare-publish only",
    "explicit human-requested surgical remove of WRONG (mfr+pid) couple"
  ],
  "contreQuoi": [
    "climate re-paint of switches/buttons",
    "market misroute shrink of irrigation/plug",
    "settings wipe on metering drivers",
    "DP1 onoff instead of meter_power on 3ph",
    "sacred couple drop after fleet enrich",
    "shallow mfr+pid-only closes without DP/flow/RX-TX (P2529)",
    "SSOT doc wipe that drops sleepy-remote / battery / pairing complementary layers (P2645)",
    "battery configureReporting re-enabled on SED remotes (degrades P2645)"
  ],
  "related": {
    "P2529": "config/architecture/deep-functional-enrich-ssot.json",
    "P2645": "config/architecture/sleepy-remote-pairing-ssot.json",
    "P2498": "config/architecture/pairing-discovery-ssot.json",
    "P2502": "config/architecture/fleet-complementary-methods-ssot.json"
  },
  "gates": [
    "check:p2519",
    "check:p2520",
    "check:p2494",
    "check:p2138",
    "check:p2645"
  ]
}
