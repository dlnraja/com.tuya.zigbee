# Deep functional enrich — ALWAYS ON (P2529)

**Mandate (user):** Treat every source (forum, GitHub, Gmail, images, portal) with **deep** RX/TX/DP/cluster/flow investigation — never stop at mfr+pid declaration.

## Always

1. Lock couple first (never invent pid).
2. Audit: clusters · DP map · RX · TX · flow compose · flow wire · capabilities · Contre quoi test.
3. Complementary (P2520): union/append only.
4. Silent enrich — no forum POST.
5. BOTH reliability → surgical stable backport when publish asked.

## Forbidden

- “Already covered” / tip-lag-only without path proof
- Compose-only flow cards left unwired
- Ignoring `cap` vs `capability` DP ownership (P2528 class)

## Commands

```bash
npm run enrich:functional
npm run check:p2529
npm run inbox:l99
```

Workflow soft-hooks: L99 inbox · forum-poll · auto-enrich · gmail-diagnostics · fetch-diags · recurrent-orchestrator · auto-bot-issue-triage.

SSOT: `config/architecture/deep-functional-enrich-ssot.json` · doctrine: `docs/rules/DEEP_FUNCTIONAL_ENRICH.md`
