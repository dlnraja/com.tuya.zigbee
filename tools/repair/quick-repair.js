const fs = require("fs"); const path = require("path"); console.log("🔧 Réparation rapide des drivers..."); const drivers = ["plug-tuya-universal", "climate-trv-tuya", "cover-curtain-tuya", "remote-scene-tuya"]; drivers.forEach(d => { const dir = `drivers/${d}`; if (fs.existsSync(dir)) { console.log(`✅ ${d} existe`); } else { console.log(`❌ ${d} manquant`); } }); console.log("::END::REPAIR_QUICK::OK");
