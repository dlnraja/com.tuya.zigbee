fix: Battery log throttling - Complete diagnostic 350b974f fixes

DIAGNOSTIC ID: 350b974f-03dc-4c02-a5cb-033cf96d14c9
USER MESSAGE: "Rien ne marche"

FINAL FIX (Missing from v4.9.41):
=====================================

BATTERY LOG SPAM THROTTLING
- Problem: Some Tuya devices (button_wireless_4) send battery updates every 10 seconds
- Despite configureAttributeReporting with minInterval: 3600s (1 hour)
- Device ignores reporting config and sends updates at its own frequency
- Result: "Battery: 100%" log spam every 10 seconds

- Solution: Add log throttling in BaseHybridDevice.js
- Track last log time with this._lastBatteryUpdate
- Only log if 60+ seconds since last log
- Battery capability STILL UPDATED (only logs throttled)

CODE CHANGE:
```javascript
// Anti-spam: Track last update time to prevent excessive logging
this._lastBatteryUpdate = 0;
const BATTERY_UPDATE_THROTTLE = 60000; // 60 seconds minimum between logs

// Inside listener callback:
const now = Date.now();
if (now - this._lastBatteryUpdate >= BATTERY_UPDATE_THROTTLE) {
  this.log(`[BATTERY] Battery: ${percentage}%...`);
  this._lastBatteryUpdate = now;
}
```

RESULT:
- Battery updates: Still processed every time
- Logs: Maximum 1 per minute
- Diagnostic reports: Much cleaner

AFFECTED FILE:
==============
- lib/BaseHybridDevice.js (lines 340-370)
- app.json (version bump: 4.9.41 → 4.9.42)

VALIDATION:
===========
✓ homey app validate --level publish: PASSED
✓ Zero errors

ALL DIAGNOSTIC 350b974f FIXES NOW COMPLETE:
============================================
1. ✅ Stack overflow (infinite recursion) - v4.9.41
2. ✅ Attribute reporting log spam (80+ logs) - v4.9.41  
3. ✅ Battery log spam (every 10s) - v4.9.42

USER RESPONSE: EMAIL_RESPONSE_350b974f.txt
