fix(sdk3): Apply latest SDK3 IAS Zone method from project

CORRECTION IMPORTANTE: Utilisation de la méthode SDK3 la plus récente

Basé sur: IASZoneEnroller_SIMPLE_v4.0.6.js (2025-10-21)
Version qui FONCTIONNE dans le projet

Changements appliqués (41 drivers):
- Utilisation de property listeners au lieu de .on() events
- Enrollment direct sans writeAttributes
- Synchronous listeners (pas async)
- Proactive enrollment response

MÉTHODE SDK3 CORRECTE (la plus récente):
✅ endpoint.clusters.iasZone.onZoneEnrollRequest = () => {...}
✅ endpoint.clusters.iasZone.zoneEnrollResponse({...})
✅ endpoint.clusters.iasZone.onZoneStatusChangeNotification = (payload) => {...}
✅ endpoint.clusters.iasZone.onZoneStatus = (zoneStatus) => {...}

MÉTHODE ANCIENNE (corrigée):
❌ endpoint.clusters[1280] (numeric)
❌ endpoint.clusters.iasZone.on('zoneStatusChangeNotification', ...)
❌ endpoint.clusters.iasZone.writeAttributes({ iasCIEAddress: ... })

DIFFÉRENCE CLEF SDK3:
- Ancien: .on('eventName', callback) - event emitter style
- Nouveau: .onEventName = callback - property assignment style

Drivers corrigés (41 total):
- Motion sensors (9)
- Contact sensors (6)
- Smoke/Gas detectors (5)
- Water detection (5)
- Climate/Environmental (6)
- Security/Access (7)
- Other devices (3)

Avantages de cette méthode:
- Plus simple (KISS principle)
- Plus fiable (synchronous)
- Plus rapide (pas de delays artificiels)
- Version testée et validée dans le projet

Référence: lib/IASZoneEnroller_SIMPLE_v4.0.6.js
Note: "Keep It Simple, Stupid" - la version simple marche mieux

Validation: homey app validate --level publish PASSED
