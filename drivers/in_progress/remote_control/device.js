try { 'use strict'; const Homey = require('homey'); const { ZigbeeDevice } = require('homey-meshdriver'); this.log('Device initialis̩'); async this.registerCapability('onoff', CLUSTER.ON_OFF); this.printNode(); 
