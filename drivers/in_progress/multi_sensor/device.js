try { 'use strict'; const Homey = require('homey'); const { ZigbeeDevice } = require('homey-meshdriver'); this.log('Device initialis̩'); async this.registerCapability('onoff', CLUSTER.ON_OFF); this.printNode(); // Capacit̩ onoff (commutateur) this.registerCapability('onoff', 'genOnOff', { getOpts: { getOnStart: true, pollInterval: 60000 } }); // Capacit̩ mesure puissance instantan̩e this.registerCapability('measure_power', 'seMetering'); // Capacit̩ ̩nergie cumul̩e this.registerCapability('meter_power', 'seMetering'); // Capacit̩ courant this.registerCapability('measure_current', 'haElectricalMeasurement'); // Capacit̩ tension this.registerCapability('measure_voltage', 'haElectricalMeasurement'); // Capacit̩ batterie this.registerCapability('measure_battery', 'genPowerCfg'); // Capacit̩ alarme batterie this.registerCapability('alarm_battery', 'genPowerCfg'); } } module.exports = multi_sensor; } catch(e) { this.error('Driver error', e); } 
