try { 'use strict'; const { ZigBeeDevice } = require('homey-meshdriver'); const { CLUSTER } = require('zigbee-clusters'); class smart_remote_4b extends ZigbeeDevice { async this.registerCapability('onoff', CLUSTER.ON_OFF); var debounce = 0; this.printNode(); const node = await this.homey.zigbee.getNode(this); node.handleFrame = (endpointId, clusterId, frame, meta) => { if (clusterId === 1281) { 
