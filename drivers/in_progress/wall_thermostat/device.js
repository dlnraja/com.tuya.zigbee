'use strict'; const TuyaSpecificCluster = require("../../lib/TuyaSpecificCluster"); const TuyaOnOffCluster = require("../../lib/TuyaOnOffCluster"); const TuyaSpecificClusterDevice = require("../../lib/TuyaSpecificClusterDevice"); const {getDataValue} = require("./helpers"); const {Cluster} = require("zigbee-clusters"); Cluster.addCluster(TuyaOnOffCluster); Cluster.addCluster(TuyaSpecificCluster); const THERMOSTAT_DATA_POINTS = { onOff: 1, mode: 2, targetTemperature: 16, currentTemperature: 24, childlock: 40 } /** * `WallThermostatDevice` makes the Tuya Wall Thermostat for Electric Floor heating (BHT-002-GCLZB) available in Homey. * The device can be set to a target temperature, turned on and off, and will show current temperature. * * Most likely also works for the Water/Gas Boiler and Water heating version too. * https: 
