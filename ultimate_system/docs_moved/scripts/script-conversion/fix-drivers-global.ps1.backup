Write-Host "🔧 CORRECTION GLOBALE DES DRIVERS..."; $drivers = @("drivers/tuya-light-universal", "drivers/tuya-plug-universal", "drivers/tuya-sensor-universal", "drivers/tuya-remote-universal", "drivers/tuya-cover-universal", "drivers/tuya-climate-universal", "drivers/zigbee-tuya-universal", "drivers/lock-tuya-universal", "drivers/fan-tuya-universal"); foreach($driver in $drivers) { if(Test-Path $driver) { Write-Host "Correction de $driver"; $composePath = "$driver/driver.compose.json"; if(Test-Path $composePath) { $content = Get-Content $composePath -Raw; $content = $content -replace "small.png", "drivers/$(Split-Path $driver -Leaf)/small.png"; $content = $content -replace "large.png", "drivers/$(Split-Path $driver -Leaf)/large.png"; Set-Content $composePath $content; Write-Host "✅ $driver corrigé !"; } } else { Write-Host "❌ $driver non trouvé"; } } Write-Host "🎉 CORRECTION TERMINÉE !";
