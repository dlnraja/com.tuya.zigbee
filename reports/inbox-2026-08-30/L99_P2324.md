# L99 inbox pass — 2026-08-30 (P2323 + P2324)

## Sources ingested
- Gmail/Homey tip emails in `.github/state/diagnostics-report.json` (100 entries; ~75 Athom build tips, ~8 real diags)
- GitHub issues **#532** (Adam) + **#533** (salvagr) + notification emails
- Athom tip: `processing_failed` / `socket hang up` (P139/P2323)
- New diag UUIDs from users: `a095345e` (#532), `7a6f2ca1` (#533) — Gmail CI fetch triggered; not yet local

## Dual-app
**BOTH** — FCU TX reliability + curtain pairing locks + publish/dashboard hang soft-alert.

## Fixes shipped in tree
| ID | What |
|----|------|
| P2323 | Dashboard Athom `$timeout` 60s + retry; soft-alert on hang+healthy Test; HOMEY_PUBLISH_DIR; tip-email signals; publish-diagnose hang hints |
| P2324 #532 | FCU: DP1/DP16 before DP101; DP101 only after RX confirm; force magic+heal; no soft-success TX |
| P2324 #533 | FPDB `5slehgeo`→`curtain_motor`; widen TRV/generic forbids (not wall_thermostat); strip `generic_tuya` mpbki compounds |

## User actions (silent GH comment)
- Adam: update Test tip after publish, retest OFF + setpoint (diag `a095345e`)
- salvagr: update Test ≥ tip, remove+re-pair as **Cover controller** / `curtain_motor` (diag `7a6f2ca1`)

## Policy
Forum: no posts. Athom hang: no bump-loop.
