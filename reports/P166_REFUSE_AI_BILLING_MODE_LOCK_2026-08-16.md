# P166 — REFUSE “AI_BILLING_MODE / zero-cost kill switch” pack (2026-08-16)

**Do not** add `AI_BILLING_MODE` secrets, rewrite `.cursorrules`, or create `quota-manager` financial kill-switches for invented AI workflows.

We already refused the multi-source auto-correct architecture (P165). A “subscription lock” does not make auto-investigate / auto-patch acceptable.

**Generate knowledge-base.json / error-patterns.json for that stack?** → **No.**
