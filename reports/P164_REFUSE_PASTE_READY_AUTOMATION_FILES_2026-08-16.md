# P164 — REFUSE paste-ready auto-* file pack (2026-08-16)

Identical pack (memory-check / auto-investigate / generate-patch / stale close / overwrite `.cursorrules`). **Do not create any of these files.**

Already have: smart `homey-heap-json-gate.js`, existing stale policy (mark-only), Auto-Publish, sacred-couple tooling, `.cursorrules`.

**Test / adjust these workflows?** → **No.**
