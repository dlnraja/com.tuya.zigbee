# VicHY #2254 treat — diag af98752d (master)

Silent. No forum POST.

## Evidence (Universal **9.0.1145**)
- Couple: `_TZE204_clrdrnya`+TS0601 → `presence_sensor_radar` (MTG075)
- EF00 alive but tile frozen; later curtain flip
- Log: `measure_battery = 100 (DP 4)` + `measure_luminance = null (DP 103)`
- Truth: DP4 = detection_range, lux = **DP104**, DP103 = cline

## Fix P2713
Generic EF00 fallback must not map DP4→battery / DP103→lux on presence radars.

## Tip
**9.0.1222** (+ P2712 Flow lag). Update Test, Repair radar once.
