# Universal Tuya Zigbee - AI Rules
# Project: com.dlnraja.tuya.zigbee | SDK: Homey SDK3 Zigbee

> [!IMPORTANT]
> **CRITICAL FIRST STEP:** You MUST read the [AI_CONTEXT_MANDATE.md](file:///c:/Users/HP/Desktop/homey%20app/tuya_repair/AI_CONTEXT_MANDATE.md) file at the very start of your session to fully digest the architecture, dual-app branch splits, and features.
> 
> **MANDATORY SECOND STEP:** Then read [GLOBAL_INVESTIGATION_PLAN.md](file:///c:/Users/HP/Desktop/homey%20app/tuya_repair/docs/GLOBAL_INVESTIGATION_PLAN.md) — the complete 22-section investigation methodology for deep diagnostic, cross-referencing forums/emails/GitHub/Z2M/ZHA, bug hunting, and prevention scripting. Every AI agent MUST follow this framework before making any code changes.
> 
> **MANDATORY THIRD STEP:** Read [ARCHITECTURE_AI.md](file:///c:/Users/HP/Desktop/homey%20app/tuya_repair/docs/ARCHITECTURE_AI.md) to understand the 3 AI layers — IDE Locale (Windows/Claude Code), GitHub Actions (workflows/ai-helper/secrets), and Homey Runtime (SDK3, zero cloud). Every AI agent MUST distinguish these layers before making automation or runtime code changes.

## CRITICAL BUGS TO AVOID

### Settings Keys (ALWAYS use these exact keys):
- `zb_model_id` NOT zb_modelId
- `zb_manufacturer_name` NOT zb_manufacturerName

### Flow Card IDs must match exactly:
- Pattern: `{driver}_physical_gang{N}_{on|off}`
- Example: `switch_2gang_physical_gang1_on`
- Check driver.flow.compose.json before using

### NO titleFormatted with [[device]] in triggers:
- Causes manual device selection bug
- Put text in `title` field instead

### Imports:
```javascript
// CORRECT:
const { ZigBeeDevice } = require('homey-zigbeedriver');
const HybridSwitchBase = require('../../lib/devices/HybridSwitchBase');
// WRONG: const TuyaZigbeeDriver = require('../../lib/TuyaZigbeeDriver');
```

### Double-Division Bug & Smart Divisors (v8.2+):
- TuyaEF00Manager auto-converts via AdaptiveDataParser (e.g. /100 for temp)
- Then dpMappings divisor divides again → wrong values
- FIX: Use `smartDivisorDetect()` for measure_temperature and measure_humidity in device classes. Do NOT hardcode `value / 100` or `value / 10` manually.

### Smart Divisor Manager (v8.2.0+)
- Located in `lib/managers/SmartDivisorManager.js`
- Auto-détection du diviseur pour temperature, humidity, co2, power, current, voltage, energy, battery, distance, luminance
- Résout le "Double-Division Bug": AdaptiveDataParser → /100 THEN dpMappings → /100 again
- `smartDivisorDetect(rawValue, dpId, options)` — détection intelligente
- `smartParse(rawValue, dpId, options)` — parse wrapper complet
- Database KNOWN_DIVISORS + VALID_RANGES pour auto-détection
- Cache automatique par (manufacturerName + dpId + capability)
- Chaîne de fallback: Known DB → Auto-detect range → defaultDivisor param → 1

## UnifiedBatteryHandler v8.2.0 (Phoenix Sovereign Fusion)
- FUSION COMPLÈTE: v8 runtime-adaptive + v5 BatteryManagerV4/V5 courbes non-linéaires
- `BATTERY_SPECS` statique: 15+ types (CR2032, CR2450, CR2477, CR123A, CR1632, AAA, AA, C, D, 9V, 2xAAA, 2xAA, 4xAAA, Li-ion, Li-polymer, 18650)
- `calculateFromVoltage(voltage, batteryType, temperature)` — interpolation linéaire multipoints + compensation température
- `lookupBatteryProfile(manufacturerName, productId)` — lookup par fabricant + productId (20+ profils)
- `DEVICE_BATTERY_MAP` — 30+ device types → battery type mapping
- `INTERVALS` — polling intervals par type d'appareil (1h à 12h)
- EMA smoothing + health monitoring (drain rate, estimated days)
- `calculateFromTuyaDP()` + `normalizeZigbeeValue()` — parsing multi-format
- Anti-flood 5min/2% deduplication
- Store persistence entre les sessions
- ⚠️ RULE: Linear formulas like `(voltage - 2.5) / 0.5` are STRICTLY BANNED
- ⚠️ RULE: Use non-linear profiles like `'3V_2100'` or `'1.5V_AA'`

## Phoenix Sovereign Architecture (v8.2.0+) Rules
1. **Virtual Buttons**: NEVER use `this.setCapabilityValue('button', ...)` natively. You MUST use `this._safeSetCapability()` or wrap with `markAppCommand()` to avoid infinite flow loops when physical buttons trigger Generic Flow Cards.
2. **Batteries**: Linear formulas like `(voltage - 2.5) / 0.5` are STRICTLY BANNED. You must use `UnifiedBatteryHandler` and non-linear profiles like `3V_2100` or `1.5V_AA`.

### Mains-Powered Sensor Devices:
- Devices with `get mainsPowered() { return true; }` must NOT have measure_battery
- Remove in onNodeInit: `await this.removeCapability('measure_battery').catch(() => {})`
- Example: _TZE200_8ygsuhe1 Smart Airbox = USB-powered

### ProductValueValidator:
- CO2 min changed from 300 to 0 (allows warmup/calibration values)
- Location: lib/ProductValueValidator.js

### ZCL Cluster Double-Parsing Bug:
- Hybrid sensors using `setupUniversalZCLListeners(this, zclNode, this.clusterHandlers)` will inherently process events.
- Never blindly call `_setupZCLCluster` inside `_setupZCLMode` unless checking `if (!this.clusterHandlers?.temperatureMeasurement)` to prevent duplicated bouncing logs and logic overlap.

### Phantom Capabilities & Fallbacks:
- If `modelId` is unknown during onboarding, do NOT fallback to a generic multi-sensor profile (like `HOBEIAN_10G_MULTI`) that forcibly assigns `hasTemperature: true`. This causes UI pollution and drops ZCL dependencies (like IAS Zone PIR motion) by setting `useTuyaDP: true` only.
- Always cascade missing model IDs safely into hybrid robust configs like `HOBEIAN_ZG204ZM_FALLBACK`.

## PATTERNS

### Mixin Order:
```javascript
class Device extends PhysicalButtonMixin(VirtualButtonMixin(HybridSwitchBase))
```

### Backlight values: `"off"`, `"normal"`, `"inverted"` (strings, not numbers)
- Must NOT use numeric comparisons (e.g., `=== 0`, `=== 1`, `'0'`, `'1'`).
- Always use ternary statements to map string states to DP integers when sending commands: `const lightMode = backlightMode === 'normal' ? 1 : 2;`
- Strictly enforced and verified by Layer 11 of the Multi-Layer Quality Gateway.

### Physical button detection (2000ms timeout):
```javascript
// State tracking (PR #120 pattern)
this._lastOnoffState = null;
this._appCommandPending = false;
this._appCommandTimeout = null;

// Mark app command:
_markAppCommand() {
  this._appCommandPending = true;
  clearTimeout(this._appCommandTimeout);
  this._appCommandTimeout = setTimeout(() => {
    this._appCommandPending = false;
  }, 2000);
}

// In DP handler - detect physical:
const isPhysical = reportingEvent && !this._appCommandPending;
if (isPhysical) {
  this.homey.flow.getDeviceTriggerCard(flowId).trigger(this, {}, {});
}
```

### BSEED ZCL-only fingerprints:
`_TZ3000_l9brjwau`, `_TZ3000_blhvsaqf`, `_TZ3000_ysdv91bk`,
`_TZ3000_hafsqare`, `_TZ3000_e98krvvk`, `_TZ3000_iedbgyxt`,
`_TZ3000_v4l4b0lp` (TS0003 3-gang switch - Issue #170)

## TUYA DP PROTOCOL
- Cluster 0xEF00 (61184)
- DP1-8: gang states | DP14: power-on | DP15: backlight | DP101: child_lock
- Types: 0=Raw, 1=Bool, 2=Value, 3=String, 4=Enum, 5=Bitmap

### Air Quality DPs (_TZE200_8ygsuhe1):
- DP2: CO2 (ppm, div 1) | DP18: Temp (div 10) | DP19: Humidity (div 10)
- DP20: PM2.5 (div 1) | DP21: VOC (ppb, div 1) | DP22: HCHO (mg/m3, div 100)

## FILES STRUCTURE
- `lib/devices/HybridSwitchBase.js` - Base class for switches
- `lib/devices/HybridSensorBase.js` - Base class for sensors (handles dpMappings divisor)
- `lib/mixins/PhysicalButtonMixin.js` - Physical button detection
- `lib/mixins/VirtualButtonMixin.js` - Virtual button support
- `lib/tuya/TuyaZigbeeDevice.js` - Base Tuya device class
- `lib/tuya/TuyaEF00Manager.js` - Tuya DP report handling + AdaptiveDataParser
- `lib/ProductValueValidator.js` - Sensor value validation ranges
- `drivers/{type}/device.js` - Device implementation
- `drivers/{type}/driver.flow.compose.json` - Flow cards definition
- `.homeycompose/capabilities/` - Custom capability definitions (measure_voc, measure_formaldehyde, etc.)

## COMMON DIAGNOSTIC ISSUES

### Duplicate Flow Triggers
Add deduplication when overriding setCapabilityValue:
```javascript
const dedupKey = `${capability}_${value}`;
const now = Date.now();
if (this._lastFlowTrigger?.[dedupKey] && now - this._lastFlowTrigger[dedupKey] < 500) {
  return; // Skip duplicate
}
this._lastFlowTrigger = this._lastFlowTrigger || {};
this._lastFlowTrigger[dedupKey] = now;
```

### TuyaZigbeeDevice Import Path
```javascript
// CORRECT:
const TuyaZigbeeDevice = require('../../lib/tuya/TuyaZigbeeDevice');
// WRONG:
const TuyaZigbeeDevice = require('../../lib/TuyaZigbeeDevice');
```

### Device Shows as "unknown/unknown"
- Check settings keys: `zb_model_id`, `zb_manufacturer_name`
- Verify fingerprints in driver.compose.json

### Double-Division in Sensor Values
- Symptom: temp 0.2°C instead of 20.6°C
- Fix: TuyaEF00Manager skips auto-convert when dpMappings divisor !== 1

### False Battery Alerts on Mains Devices
- Set `get mainsPowered() { return true; }` in device.js
- Remove measure_battery in onNodeInit

### Sensor Value Rejected by Validator
- Check ProductValueValidator.js min/max ranges

## SDK v3 BATTERY CAPABILITIES — RUNTIME ADAPTIVE

### Rule: NEVER combine measure_battery + alarm_battery on same device
- But BOTH should be declared as POSSIBLE in compose for hybrid variants
- `UnifiedBatteryHandler.js` (lib/battery/) adapts at RUNTIME:
  - Probes ZCL genPowerCfg, Tuya DP, IAS Zone, all simultaneously
  - If device reports % → keeps `measure_battery`, removes `alarm_battery`
  - If device reports boolean low/ok only → keeps `alarm_battery`, removes `measure_battery`
  - If mains/kinetic → removes both
  - Same driver, different variants = different runtime behavior

### Why RUNTIME, not STATIC:
- Chinese manufacturers make exotic implementations
- Same manufacturerName: variant A = battery, variant B = mains, variant C = kinetic
- Some devices report battery from 3 sources simultaneously (ZCL + Tuya DP + IAS Zone)
- Some devices switch protocol mid-session
- Static compose cannot predict which variant will be paired
- UnifiedBatteryHandler handles ALL cases via runtime probing

### Energy source detection order (UnifiedBatteryHandler):
1. ZCL genPowerCfg (cluster 0x0001) → batteryPercentageRemaining ÷ 2
2. Tuya DP (DP 4,10,14,15,21,100-105) → direct percentage
3. IAS Zone Status bit 3 → low-battery boolean
4. Voltage DPs (DP 33,35,247) → voltage-to-percent curve conversion
5. mainsPowered getter → remove all battery capabilities
6. Kinetic detection (TS004x without batteries) → remove all

## DEVICE POWER SOURCE CLASSIFICATION (all cases)

### 1. Battery-Powered (class: sensor, remote)
- Has `energy.batteries` in compose → MUST have ONE battery cap
- Reports % → `measure_battery` | Reports boolean → `alarm_battery`
- Examples: motion_sensor, contact_sensor, temphumidsensor, remotes

### 2. Mains-Powered (class: socket, light, fan)
- NO battery capability ever
- `get mainsPowered() { return true; }` in device.js
- Remove measure_battery if inherited: `removeCapability('measure_battery')`
- Examples: wall switches, plugs, bulbs, LED strips, dimmers, din_rail

### 3. Hybrid (mains + battery backup)
- Some wall-mounted sensors run on BOTH mains AND battery
- Same manufacturerName can have mains AND battery variants
- MUST check at runtime: `mainsPowered` getter + conditional capability
- Example: Some _TZ3000_ wall remotes are battery, others are hardwired

### 4. Self-Powered / Kinetic / Mechanical
- Energy harvesting from button press click (no battery, no mains)
- NO battery capability, NO energy.batteries in compose
- Examples: Tuya wireless buttons (TS0041-TS0046), some scene switches
- These devices use cluster 0xE000 or onOff commands only
- Fingerprint: same _TZ3000_ prefix but productId TS004x

### 5. USB-Powered (mains equivalent)
- Treated exactly like mains: NO battery capability
- `get mainsPowered() { return true; }`  
- Examples: air quality sensors (_TZE200_8ygsuhe1), IR blasters

## ZIGBEE PROTOCOL CLASSIFICATION (all cases)

### 1. Tuya DP Protocol (TS0601 / cluster 0xEF00)
- productId = TS0601 (always)
- Cluster 61184 (0xEF00) for data exchange
- Uses TuyaEF00Manager for DP parsing
- dpMappings in device.js maps DP numbers to capabilities
- Thousands of variants: same _TZE200/204/284_ prefix = DP device
- EACH manufacturerName may have DIFFERENT DP mappings!

### 2. Standard ZCL (native Zigbee clusters)
- productId = TS0001-TS0504, TS011F, TS0121, etc.
- Uses standard clusters: onOff(6), levelControl(8), colorControl(768)
- No TuyaEF00Manager needed
- configureAttributeReporting() for periodic updates
- Most reliable protocol type

### 3. Tuya Standard Zigbee (ZCL + Tuya extensions)
- productId = TS0001-TS0044, TS011F
- Standard ZCL clusters PLUS Tuya-specific attributes
- May have cluster 0xE000 (57344) for button press types
- May have cluster 0xE001 (57345) for Tuya-specific settings
- Backlight, power-on-state via Tuya DPs on cluster 0xEF00

### 4. Custom Zigbee (manufacturer-specific clusters)
- Non-standard clusters (0xFC00-0xFCFF)
- Requires raw frame interception in device.js
- Example: some LIDL/Silvercrest devices

## VARIANT INTELLIGENCE (manufacturerName × productId matrix)

### One manufacturerName → Many productIds (NORMAL)
```
_TZ3000_abc + TS0001 → switch_1gang
_TZ3000_abc + TS0002 → switch_2gang
_TZ3000_abc + TS0043 → button_wireless_3
```
Same OEM manufactures different product types.

### One productId → Many manufacturerNames (NORMAL)
```
TS0601 + _TZE200_xxx → thermostat
TS0601 + _TZE200_yyy → soil_sensor
TS0601 + _TZE284_zzz → presence_sensor_radar
```
TS0601 is a generic container; the manufacturerName determines function.

### Same manufacturerName + Same productId → MUST be same driver
If same combo maps to different drivers = CONFLICT. Fix by:
1. Check Z2M/ZHA for correct device type
2. Move to correct driver
3. Keep in only ONE driver

### Power Source Varies Per Variant!
```
_TZ3000_abc + TS0041 → kinetic button (no battery)
_TZ3000_abc + TS0042 → kinetic 2-button (no battery)
_TZE200_def + TS0601 → battery soil sensor (measure_battery)
_TZE200_ghi + TS0601 → USB air quality (mainsPowered=true)
```
NEVER assume power source from manufacturerName alone.
Check productId + device class + energy config.

## CRITICAL: Fingerprint Matching Rules

### manufacturerName CAN appear in MULTIPLE drivers - THIS IS NORMAL!

**Fingerprint = manufacturerName + productId (COMBINED)**
```
_TZ3000_abc + TS0001 → switch_1gang
_TZ3000_abc + TS0002 → switch_2gang  (SAME mfr, different productId = OK!)
_TZ3000_abc + TS0003 → switch_3gang
_TZ3000_abc + TS0012 → dimmer_2gang
```

### When to REMOVE a fingerprint:
- ONLY if SAME manufacturerName + SAME productId causes WRONG driver match
- Do NOT remove just because mfr appears in another driver with different productId

### Common Multi-Driver Manufacturers:
- `_TZ3000_*` - Often makes 1-gang, 2-gang, 3-gang switch variants
- `_TZE200_*` / `_TZE204_*` - Tuya DP devices across sensors, TRVs, covers
- Same OEM supplies different productIds for different device types

### Matching Logic in driver.compose.json:
```json
{
  "zigbee": {
    "manufacturerName": ["_TZ3000_abc", "_TZ3000_xyz"],
    "productId": ["TS0002", "TS0012"]
  }
}
```
Device matches if manufacturerName IN list AND productId IN list.

## GITHUB ACTIONS / YML WORKFLOW RULES

> Full reference: `.github/WORKFLOW_GUIDELINES.md` and `.github/SECRETS.md`

### Before editing ANY `.github/workflows/*.yml`:
1. Read `.github/WORKFLOW_GUIDELINES.md` first
2. Every job running `node` scripts MUST have: checkout → setup-node → npm ci
3. Always set `timeout-minutes` on jobs
4. Always set `permissions:` and `concurrency:` at top level
5. Use `continue-on-error: true` for optional-secret steps
6. Use `[skip ci]` in auto-commit messages to prevent infinite loops
7. Stagger cron schedules by 30+ min to avoid git push conflicts
8. Draft→test promotion needs 3 tiers: Node script → curl fallback → verify retry
9. `DISCOURSE_API_KEY` lint warnings are false positives — secret exists
10. `GITHUB_TOKEN` is auto-provided; `GH_PAT` is for cross-repo access
11. Guard missing secrets: `if [ -z "$SECRET" ]; then exit 0; fi`
12. Git commits: always `git pull --rebase origin master || true` before push

### Secret Priority:
1. `HOMEY_PAT` — publishing (https://tools.developer.homey.app/me)
2. `GH_PAT` — cross-repo (scopes: repo, read:org)
3. `GOOGLE_API_KEY` — AI analysis
4. `DISCOURSE_API_KEY` — forum posting
5. `HOMEY_EMAIL` + `HOMEY_PASSWORD` — forum login fallback

## EXTERNAL SYNC SOURCES
- Z2M: github.com/Koenkk/zigbee2mqtt/issues (`_TZE284_`)
- ZHA: github.com/zigpy/zha-device-handlers/issues
- JohanBendz: github.com/JohanBendz/com.tuya.zigbee/pulls (redirect users to dlnraja fork)

## GITHUB ISSUE RESPONSE PATTERN
1. Check if fingerprint already in dlnraja fork
2. Post: "Supported in dlnraja/com.tuya.zigbee v{version}"
3. Ask for diagnostic report + interview if needed
4. After fix: update comment with fix details

## VERSION RELEASE
1. `npx homey app validate` → publish level
2. Update `.homeycompose/app.json` version
3. Add to `.homeychangelog.json` (TOP)
4. `git commit && git push`

## FLOW CARD RULES
- Trigger: `getDeviceTriggerCard()` with empty args
- Condition driver-level: `getDeviceConditionCard()`
- Condition app-level: `getConditionCard()`
- NO `[[device]]` in titleFormatted for triggers
- ALWAYS use `this._safeSetCapability()` rather than `this.setCapabilityValue()` natively on virtual endpoints so custom triggers (e.g. `switch_3gang_gangX_turned_on`) propagate correctly.

## GITHUB ISSUES / PR HANDLING
- NEVER auto-close issues or PRs until fully resolved and deeply verified
- NEVER dismiss or disaffirm user reports without thorough investigation
- Always research device capabilities from product sheets, manufacturer specs, Z2M, ZHA, Blakadder
- Cross-reference manufacturerName, productId, DPs, ZCL clusters, capabilities with real sources
- A device that "compiles" is NOT the same as a device that "works correctly"

## DRIVER MATCHING (v5.11.138 audit)
- Homey SDK: EXACT string match on driver.compose.json fingerprints
- universal_fallback: 0 conflicts with specific drivers (verified)
- PermissiveMatchingEngine.js: DEAD CODE, not imported anywhere
- package.json version must sync with app.json (GitHub Actions do this)

## SCENE SWITCH AUDIT (Z2M cross-reference)
- _TZ3000_ prefix = ZCL devices, report TS004x productIds, use cluster 57344 (0xE000)
- _TZE200_/_TZE204_ prefix = Tuya DP devices, report TS0601, use cluster 61184 (0xEF00)
- Real productId mapping: TS0041=1btn, TS0042=2btn, TS0043=3btn, TS0044=4btn, TS0046=6btn
- 3 driver families: button_wireless (ZCL primary), wall_remote (ZCL dup), scene_switch (DP only)
- DO NOT add TS004x to scene_switch (conflicts with button_wireless)
- FIXED: _TZ3000_bi6lpsew removed from scene_switch_6 (TS0043, wrong driver)
- FIXED: All scene switches got cluster 57344 (0xE000) for E000 detection

## SDK v3 OFFICIAL RULES (source: apps-sdk-v3.developer.homey.app)

### Manager Access Pattern (MANDATORY)
```javascript
// SDK v3: Access managers through this.homey.<managerId>
this.homey.drivers.getDriver('my-driver');  // ManagerDrivers
this.homey.flow.getDeviceTriggerCard('id'); // ManagerFlow
this.homey.settings.get('key');             // ManagerSettings

// WRONG (SDK v2 global Managers — will crash):
// Homey.ManagerDrivers, ManagerSettings, etc.
```

### Async Requirements (MANDATORY)
- `async onNodeInit()` — Zigbee device initialization (NOT onInit)
- `await this.setCapabilityValue()` — MUST be awaited
- `async onPair()`, `async onDiscovery()` — all pairing methods async
- NO `.then()` callbacks for SDK v3 methods — use async/await

### Memory Management (Homey Pro 2023 critical)
- Every `registerCapabilityListener` must be idempotent on restart
- Avoid listener duplication when device reconnects/restarts
- Clear timeouts in `onDeleted()` or `onUninit()`
- Max ~8 listeners per device before memory review needed

### CI Semantic Linter Safety
- NEVER auto-modify driver files in CI — report-only mode
- Don't blindly pipe AI output into source files
- SDK v3 linter checks: async init, Manager globals, v2 API, await setCapability, listener leaks, settings keys, titleFormatted

## VISIBILITY MATRIX (Ghost Mode vs Public Mode)

### SILENT sources (implement code, hide attribution):
- Johan Bendz GitHub (JohanBendz/com.tuya.zigbee)
- Forum T26439, T146735, T89271, all non-T140352 threads
- Tuya WiFi threads, external forks, community PRs
- Zigbee2MQTT, ZHA, deConz discoveries
- → Code: YES (implement) | Logs/Changelog: NO | Forum: NO | Commits: generic wording

### PUBLIC sources (full visibility):
- Forum T140352 (dlnraja's own thread)
- dlnraja's GitHub issues/PRs
- Direct user requests from T140352
- All descendant forks of dlnraja/com.tuya.zigbee
- → Code: YES | Logs: YES | Changelog: YES | Forum: YES (merge with last post)

### Forum Ghostwriter Rules:
- If last poster = dlnraja: AI edits/merges into existing post (organic fusion)
- If last poster = someone else: AI creates new reply (only on T140352)
- NEVER consecutive bot posts — always merge with own last post
- Ghostwriter tone: casual, like a dev editing their own post ("edit: just pushed...")
- NEVER expose: automation, bots, AI, pipelines, Johan, upstream sync

## DYNAMIC APP PROMOTION & INTELLIGENT TOOLS

### Parallel Release Tracks (Dual Release Guard)
- **Live Branch (`master`)**: Compiles to App ID `com.dlnraja.tuya.zigbee` (Production / SDK v7.x).
- **Parallel Testing Branch (`stable-v5`)**: Compiles to App ID `com.dlnraja.tuya.zigbee.stable` (v5 parallel).
- Critical fixes and library improvements must be synchronized across both tracks.

### Dynamic App Resolution in Automation
- Never hardcode App IDs or release namespaces in automated scripts or deployment helpers (e.g., `auto-publish-draft.js`, `auto-promote-oauth.js`, `auto-promote-puppeteer.js`).
- Always load and parse `app.json` dynamically at runtime to resolve the active App ID and compiled version.

### Casing Normalization
- Always run `scripts/maintenance/master-self-heal.js` to normalize case-variants and prevent duplicates.

### Internet Research Scraper Tool
- For web searching and workspace resource indexing without local browser/web server engines, utilize `.github/scripts/internet-research-tool.js`:
  - `node .github/scripts/internet-research-tool.js search "query"` (DuckDuckGo scraping)
  - `node .github/scripts/internet-research-tool.js urls` (Workspace reference URL extractor)
  - `node .github/scripts/internet-research-tool.js fetch "url"` (Webpage-to-Markdown)
  - `node .github/scripts/internet-research-tool.js scan "term"` (Full-text file searcher)

## POST-PROMOTION DOCUMENTATION & REGISTRY SYNC
- **Mandatory Protocol**: Refer to [POST_PROMOTION_PROTOCOL.md](file:///c:/Users/HP/Desktop/homey%20app/tuya_repair/docs/rules/POST_PROMOTION_PROTOCOL.md) for the mandatory recursive audit, enrichment loop, and technical safeguards (Layer 12 compliance).

Generated by Antigravity AI — 13 May 2026 | Updated 2026-05-30 | Version 8.1.x


## 📦 ARCHIVE SIZE & PUBLISH RULES (CRITICAL — v8.1.7+ Root Cause)

> [!CAUTION]
> **ROOT CAUSE DISCOVERED 2026-05-30**: Every build from v8.1.7 to v8.1.26 showed
> "Processing failed" on Athom because `.diag/` was NOT in `.homeyignore`.
> This alone added 55MB to the archive → total ~39MB compressed → Athom rejection.
> **Fix applied**: Added `.diag/` `.memory/` `.ai/` `.gemini/` to `.homeyignore`.

### Archive Size Thresholds
| Level | Limit | Action |
|-------|-------|--------|
| ✅ Target | < 20 MB compressed | Safe publish zone |
| ⚠️ Warning | 15–20 MB compressed | Review what's included |
| ❌ Block | > 20 MB compressed | CI gate blocks publish |
| 💥 Athom hard limit | ~30 MB compressed | Processing fails silently |

### Folders that MUST be in .homeyignore (never ship to Athom)
```
.diag/           ← CI screenshots, tarballs (55MB bloat — ROOT CAUSE)
.memory/         ← AI session memory
.ai/             ← AI config
.gemini/         ← Gemini AI config
.archive/        ← Local backups
reimplementation_gateway/  ← Dev videos/assets (40MB MP4!)
screenshots/     ← Local Puppeteer screenshots
docs/            ← Documentation
reports/         ← Audit reports
data/archive/    ← Old data
.github/         ← CI/CD scripts
node_modules/    ← Dependencies
.homeybuild/     ← Build artifacts
```

### Folders REQUIRED in the archive (runtime deps — DO NOT exclude)
```
app.json                          ← Homey manifest
app.js                            ← App entry point
package.json                      ← Node package descriptor
driver-mapping-database.json      ← DriverMappingLoader.js runtime dep
data/fingerprints.json            ← DeviceFingerprintDB.js (lazy-loaded)
lib/tuya/fingerprints.json        ← tuya-engine runtime dep
locales/*.json                    ← i18n translations
assets/icon.svg                   ← Required by Athom store
assets/images/small.png           ← Required store image
assets/images/large.png           ← Required store image
drivers/*/assets/icon.svg         ← Per-driver icon
drivers/*/assets/images/small.png ← Per-driver image
drivers/*/assets/images/large.png ← Per-driver image
README.txt                        ← Required by SDK (App.js:1427)
README.nl.txt                     ← App Store NL description
README.de.txt                     ← App Store DE description
README.fr.txt                     ← App Store FR description
.homeychangelog.json              ← Store changelog
```

### CI Pre-Publish Checks (enforced in publish.yml)
1. `node .github/scripts/check-archive-size.js` — exit 1 if > 20MB
2. `node .github/scripts/athom-archive-audit.js --ci` — full audit
3. Archive gate in `version-and-publish` job blocks publish if > 20MB compressed
4. `timeout-minutes: 22` on version-and-publish job (was 15 → cancelled Tier 3)

### Puppeteer SPA Warm-up (CRITICAL FIX 2026-05-30)
- **BUG**: Conditional `if (!page.url().includes('/apps'))` caused blank /versions page
- **FIX**: Always `await page.goto(myAppsUrl)` unconditionally after login
- **Evidence**: screenshot 05-app0-versions.png was 59KB (blank) → now 283KB (content)
- **Tier 1 timeout**: 5min — warm-up reduced to 4s sleep to stay within budget

### Run Scripts
```bash
# Check archive size locally
node .github/scripts/check-archive-size.js

# Full audit with bloat detection
node .github/scripts/athom-archive-audit.js

# Check Athom API current test version
curl https://apps-api.athom.com/api/v1/app/com.dlnraja.tuya.zigbee | jq '.testVersion'
```




## 6. DEPRECATED PATTERNS (NO_BATTERY_MIXIN, NO_TUYA_ZIGBEE_EXTEND)
- **NEVER** use BatteryMixin. It has been completely replaced by UnifiedBatteryHandler natively embedded in BaseUnifiedDevice and UnifiedSensorBase.
- **NEVER** extend TuyaZigbeeDevice directly if you need to use this.sendTuyaCommand() or this._tuyaEF00Manager. Always extend BaseUnifiedDevice or UnifiedSensorBase.
\n

## Dynamic RAM Optimization & Dual-Layer Pairing (v9.0.0+)
- **Buffer-Based Direct JSON Loading (Double Heap memory fix)**: For all large JSON/file database loading (like `fingerprints.json` or `driver-mapping-database.json`), you **MUST** read the file directly as a raw Buffer (`fs.readFileSync(fpath)`) instead of converted UTF-16 string (`utf8`). Node.js allows passing a Buffer directly to `JSON.parse(buffer)`. This avoids allocating a giant intermediate JS String on the V8 heap and prevents fatal OOM crashes under Homey Pro's strict 64MB memory limit.
- **Defensive V8 Garbage Collection**: The loader forces `global.gc()` sweeps (if available) before and after database parses to cleanly eject transient chunks.
- **Static vs Dynamic Dual-Layer Matching**:
  - **Static Manifests**: The manufacturer name, ZCL clusters, and `modelId` must remain statically registered in `driver.compose.json` / `app.json` to allow the Homey Pro box to match the hardware and complete the pairing flow locally.
  - **Dynamic JSON Databases**: Kept in the app bundle (fully unignored in `.homeyignore`) to refine capabilities and custom DP parser operations dynamically at runtime, ensuring offline capability mapping continues to work perfectly.

## Fleetwood Gateway & Syntax Purity Mandates (v8.4.0+)
- **ZCL Method Override Purity**: Any driver class override of ZCL/SDK methods (e.g., `getDeviceById(id)`) MUST have correct closing braces to prevent breaking following method declarations like `onInit()` or `onNodeInit()`.
- **Standard ZCL Measurement Scoping**: Inside ZCL Measurement cluster listeners (`attr.rmsCurrent`, etc.), never re-declare block-scoped variables (`const scaled`) in the same scope block; use unique variables (`scaledCurrent` and `scaledPower`) to prevent standard JS scope crash exceptions.
- **Mains/Battery Approximation Conflicts**: If a compose manifest (`driver.compose.json`) defines `measure_power` or `meter_power`, it MUST NOT define `energy.approximation` under `energy` to prevent severe Homey Pro Energy v3 schema validation errors.
- **Mandatory GitHub Actions Shell Default Configuration**: Every CI workflow `.yml` file that runs commands MUST include top-level `defaults: run: shell: bash` to prevent PowerShell syntax failures on Windows runners.
- **GHA Cloud Purity Validation Gate**: Every primary workflow CI check (`unified-ci.yml`, `syntax-validation.yml`, `syntax-purity-gate.yml`, `syntax-check.yml`, `validate.yml`) has been hardened to execute the strict `PRE_COMMIT_CHECKS.js` gateway. This ensures 100% JS syntax parsing, workflow default checking, and manifest schema validation before commit integration.

// ============================================================================
// DEEP DIAGNOSTIC & CROSS-REFERENCING MANDATE (CRITICAL)
// ============================================================================
- DO NOT just say 'Missing manufacturer'. Perform deep cross-referencing with Z2M/ZHA and scrape forum/email info.
- Remember a single `manufacturerName` can map to MULTIPLE variants and device IDs. Check productId!
- Use Antigravity skills (`.agents/skills/`) and local Claude Code scripts.
- Update docs, workflows, and Github Actions after fixing a bug.
- Read Architecture 7+ and 8.0 docs before acting.

// ============================================================================
// AUTONOMOUS AGENT REFLECTION & LOGIC MODE
// ============================================================================
- **Thinking Blocks**: Before taking action, use a structured thinking process to reflect on the codebase state and potential side-effects.
- **Incremental Verification**: Do not attempt to fix 10 things at once. Plan, execute one logical block, test/verify, and reflect on the outcome before proceeding.
- **Tool-Driven Loop**: Leverage tools intelligently (read files to confirm context, search codebase for dependencies, run validation scripts) before making blind code edits.
- **Context Mastery**: Assimilate the dual-app context and project architecture fully into your reasoning model before proposing solutions.

// ============================================================================
// FORUM MONITORING & ISSUE RESOLUTION
// ============================================================================
- **Forum Thread 140352**: Universal TUYA Zigbee Device App (2011+ posts). Last activity: June 9, 2026.
- **Forum Thread 26439**: JohanBendz Tuya Zigbee App (1153 posts). Last activity: May 2026.
- **Scraping Tools**: Use `scripts/automation/enhanced-forum-scraper.js` for automated monitoring.
- **Manual Processing**: Use `scripts/_process_forum.js` for manual post processing.
- **Changelog Reading**: ALWAYS read `.homeychangelog.json` before fixing any issue.
- **Diagnostics**: Ask user to share via https://tools.developer.homey.app
- **Resolution Comments**: Post with version number, root cause, files modified, test instructions.

// ============================================================================
// DEVICE-SPECIFIC RULES (from forum/issues investigation)
// ============================================================================
- **Bed Sensor (_TZE200_seq9cm6u)**: DP1=trueFalse0, DP4=batteryConfig (NOT dpMappings), DP12=pressure, requestDP singular, _handleBatteryDP override for 0/1 mapping
- **Illuminance (_TZE284_aaeasoll)**: DP2=illuminance (Z2M PR #12347), NOT DP3/DP4/DP12
- **Rain Sensor (_TZE200_u6x1zyv2)**: Hybrid IAS Zone + Tuya DP, both protocols handled
- **Radiator Valve (_TZE200_9xfjixap)**: Must be in radiator_valve, NOT device_air_purifier_radiator
- **Soil Sensor (_TZE284_oitavov2)**: DP3=moisture, DP5=temperature (auto-scale), DP14=battery_state enum
- **4-Button Remote (_TZ3000_u3nv1jwk)**: TS0044 scene mode, 4 endpoints = 4 buttons, only in button_wireless_4
- **Presence Radar (_TZE200_ka8l86iu)**: DP1=presence, DP106=illuminance, DP110=battery (non-standard DP)
