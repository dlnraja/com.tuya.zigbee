/**
 * Common utilities index
 */

const diagnostics = require('./diagnostics');
const deviceHelpers = require('./device-helpers');
const naming = require('./naming');

module.exports = {
  diagnostics,
  deviceHelpers,
  naming
};
