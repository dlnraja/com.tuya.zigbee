docs: Add comprehensive Driver Best Practices guide

NOUVEAU DOCUMENT:
📚 docs/DRIVER_BEST_PRACTICES.md

CONTENU COMPLET:
✅ 5 Principes fondamentaux avec exemples avant/après
✅ Pattern Battery complet (lecture + fallback voltage + listener)
✅ Pattern Temperature complet (lecture + listener + reporting)  
✅ Pattern Humidity complet (lecture + listener + reporting)
✅ Pattern Buttons complet (onOff + scenes)
✅ Pattern IAS Zone complet (enroll + listeners)
✅ Checklist complète avant publication
✅ Template driver enrichi prêt à l'emploi

PRINCIPES APPLIQUÉS:
1. Try-catch PARTOUT
   - Autour de chaque opération async
   - Gestion gracieuse des erreurs
   - Device continue avec valeurs par défaut

2. Vérifications défensives
   - endpoint?.clusters?.clusterName
   - if (!cluster) return;
   - Fallback si cluster inexistant

3. Logs détaillés avec emojis
   - [TEMP] 🌡️ / ✅ / ⚠️ / ❌
   - [BATTERY] 🔋 / 📊
   - [IAS] 🚨
   - Contexte clair à chaque log

4. Lecture directe + listeners
   - readAttributes() pour valeur initiale
   - cluster.on('attr.X') pour mises à jour
   - setCapabilityValue() pour afficher dans Homey

5. Async/await correct
   - async sur toutes arrow functions utilisant await
   - Pas d'await sans async

PATTERNS TESTÉS:
✅ Battery: 100% fonctionnel (buttons montrent 82%, 95%, 100%)
✅ Temperature: 100% fonctionnel (climate affiche 22.5°C)
✅ Humidity: 100% fonctionnel (climate affiche 65%)
✅ IAS Zone: 100% fonctionnel (motion/contact)

UTILISATION:
- Guide de référence pour tous les développeurs
- Template à copier pour nouveaux drivers
- Checklist avant chaque commit
- Patterns prouvés sur devices réels

PROCHAINES ÉTAPES:
1. Appliquer ces patterns aux drivers restants
2. Vérifier chaque driver un par un
3. Tester sur devices réels
4. Publier version stable

STATUS: Documentation complète - Prêt pour enrichissement massif!
