# DISABLED v8.5.0 — Redundant with unified-ci.yml
name: Check Invalid Paths
on:
  push:
    branches: [master, stable-v5]

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  placeholder:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "DISABLED — merged into unified-ci.yml"