# e2e-dashboard-test
# Triggers: cron: 0 7 * * * + workflow_dispatch,workflow_run | Secrets: GITHUB_TOKEN only
# Doc header added 2026-07-29 (workflows genealogy enrichment).

defaults:
  run:
    shell: bash

# End-to-End Dashboard Test
#
# Runs after every continuous-flow + on push to master.
# Tests BOTH apps (master v9.x + stable v5.x) for:
#   - AggregateError (caused by empty mfrName in driver.compose.json)
#   - process errors (uncaught exceptions, unhandled rejections)
#   - driver health score
#   - validation pass rate
#
# Posts results as PR comment + workflow summary.

name: e2e-dashboard-test

on:
  schedule:
    # 4h after continuous-flow (07:00 UTC) — gives time for enrich to finish
    - cron: '0 7 * * *'
  workflow_dispatch:
  workflow_run:
    workflows: ['continuous-flow']
    types: [completed]

concurrency:
  group: e2e-dashboard-test
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      issues: write
      pull-requests: write
    env:
      NODE_VERSION: '22'

    steps:
      - name: Checkout master
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          path: master
          fetch-depth: 5
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'master/package-lock.json'

      - name: Install master deps (cached)
        working-directory: master
        run: npm ci --prefer-offline --no-audit
        if: hashFiles('master/package-lock.json') != ''

      - name: Validate master (AggregateError + process errors)
        id: validate-master
        working-directory: master
        run: |
          echo "### Master validation ###"
          set +e
          OUTPUT=$(npx homey app validate --level publish 2>&1)
          EXIT=$?
          set -e
          echo "$OUTPUT" > /tmp/master-validate.txt
          # Always show the last lines of the validator output on failure —
          # without this the failure reason is invisible in CI logs.
          if [ $EXIT -ne 0 ]; then
            echo "::group::Validator output (tail)"
            echo "$OUTPUT" | tail -30
            echo "::endgroup::"
          fi
          if [ $EXIT -ne 0 ]; then
            echo "::error::Master validation FAILED (exit $EXIT)"
            # Detect specific errors
            if echo "$OUTPUT" | grep -q "AggregateError"; then
              echo "AGGREGATE_ERROR_DETECTED=true" >> $GITHUB_OUTPUT
            fi
            if echo "$OUTPUT" | grep -qE "Error:|TypeError:|RangeError:|SyntaxError:"; then
              echo "PROCESS_ERROR_DETECTED=true" >> $GITHUB_OUTPUT
            fi
            exit $EXIT
          fi
          echo "Master validation PASS"

      - name: Bug hunter (master)
        id: bug-master
        working-directory: master
        run: |
          if [ -f scripts/ci/bug-hunter.js ]; then
            node scripts/ci/bug-hunter.js 2>&1 | tee /tmp/master-bugs.txt || true
          else
            echo "(bug-hunter.js not present, skipping)"
          fi

      - name: Driver health (master)
        id: health-master
        working-directory: master
        run: |
          if [ -f scripts/validation/check-driver-health.js ]; then
            node scripts/validation/check-driver-health.js 2>&1 | tee /tmp/master-health.txt || true
          else
            echo "(check-driver-health.js not present, skipping)"
          fi

      - name: 'Checkout stable (P75.11 use git worktree to actually fetch stable-v5)'
        id: checkout-stable
        run: |
          # P75.11: The previous version only did `mkdir -p stable` and left it empty,
          # which caused the validator to fail with:
          #   "Could not find a valid Homey App at /path/stable/app.json: ENOENT"
          # Now we use `git worktree add` to actually check out the stable-v5 branch.
          mkdir -p stable
          git fetch --depth=1 origin stable-v5:refs/remotes/origin/stable-v5 2>/dev/null || true
          if git worktree add --detach stable origin/stable-v5 2>/dev/null; then
            echo "Checked out stable-v5 branch into stable/"
            echo "STABLE_AVAILABLE=true" >> $GITHUB_OUTPUT
          else
            # Fallback: use the saved stable_app.json snapshot from master
            if [ -f master/backups/stable_app.json ]; then
              echo "::warning::Could not checkout stable-v5 worktree, using stable_app.json from master"
              cp master/backups/stable_app.json stable/app.json
              if [ ! -f stable/package.json ]; then
                printf '%s' '{"name":"com.dlnraja.tuya.zigbee.stable","version":"5.12.4","main":"app.js"}' > stable/package.json
              fi
              echo "STABLE_AVAILABLE=true" >> $GITHUB_OUTPUT
            else
              echo "::warning::No stable source available, skipping stable validation"
              echo "STABLE_AVAILABLE=false" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Validate stable (AggregateError + process errors)
        id: validate-stable
        run: |
          if [ "${{ steps.checkout-stable.outputs.STABLE_AVAILABLE }}" != "true" ]; then
            echo "(stable not available, skipping)"
            exit 0
          fi
          if [ ! -f stable/app.json ]; then
            echo "(stable/app.json missing after checkout, skipping)"
            exit 0
          fi
          cd stable
          set +e
          OUTPUT=$(npx homey app validate --level publish 2>&1)
          EXIT=$?
          set -e
          echo "$OUTPUT" > /tmp/stable-validate.txt
          if [ $EXIT -ne 0 ]; then
            echo "::error::Stable validation FAILED (exit $EXIT)"
            if echo "$OUTPUT" | grep -q "AggregateError"; then
              echo "STABLE_AGGREGATE_ERROR=true" >> $GITHUB_OUTPUT
            fi
            if echo "$OUTPUT" | grep -qE "Error:|TypeError:|RangeError:|SyntaxError:"; then
              echo "STABLE_PROCESS_ERROR=true" >> $GITHUB_OUTPUT
            fi
          else
            echo "Stable validation PASS"
          fi

      - name: Build summary
        if: always()
        env:
          # Pass trigger ref through env to avoid shell injection from branch names
          TRIGGER_REF: ${{ github.event.schedule || github.event.workflow_run.head_branch || '' }}
        run: |
          {
            echo "## E2E Dashboard Test - $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo ""
            echo "### Master (v9.x)"
            echo "- Validation: ${{ steps.validate-master.outcome == 'success' && 'PASS' || 'FAIL' }}"
            echo "- Bug hunter: ${{ steps.bug-master.outcome == 'success' && 'OK' || 'WARN' }}"
            echo "- Driver health: ${{ steps.health-master.outcome == 'success' && 'OK' || 'WARN' }}"
            echo ""
            echo "### Stable (v5.x)"
            echo "- Validation: ${{ steps.validate-stable.outcome == 'success' && 'PASS' || 'FAIL' }}"
            echo ""
            echo "**Run ID**: ${{ github.run_id }}"
            echo "**Trigger**: ${{ github.event_name }} $TRIGGER_REF"
          } >> $GITHUB_STEP_SUMMARY

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        with:
          name: e2e-dashboard-reports-${{ github.run_id }}
          path: |
            /tmp/master-validate.txt
            /tmp/master-bugs.txt
            /tmp/master-health.txt
            /tmp/stable-validate.txt
          if-no-files-found: ignore
          retention-days: 14

      - name: Notify on AggregateError
        if: steps.validate-master.outputs.AGGREGATE_ERROR_DETECTED == 'true' || steps.validate-stable.outputs.STABLE_AGGREGATE_ERROR == 'true'
        run: |
          echo "::error::AggregateError detected in one or both apps."
          echo "Master: ${{ steps.validate-master.outputs.AGGREGATE_ERROR_DETECTED }}"
          echo "Stable: ${{ steps.validate-stable.outputs.STABLE_AGGREGATE_ERROR }}"
          echo "Run tools/ci/fix-empty-mfr-aggregateerror.js to fix."

      - name: Notify on process errors
        if: steps.validate-master.outputs.PROCESS_ERROR_DETECTED == 'true' || steps.validate-stable.outputs.STABLE_PROCESS_ERROR == 'true'
        run: |
          echo "::error::Process error detected in one or both apps."
          echo "Master: ${{ steps.validate-master.outputs.PROCESS_ERROR_DETECTED }}"
          echo "Stable: ${{ steps.validate-stable.outputs.STABLE_PROCESS_ERROR }}"
