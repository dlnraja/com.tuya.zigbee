# sync-master-beta.yml
# Description: Workflow automatisÃ© pour sync master beta
# Date: 2025-07-25
# Version: 1.0

name: Sync master â†’ beta

# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on:
  schedule:
    - cr
# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: '0 0 1 * *' # chaque 1er du mois
  workflow_dispatch:
jobs:
  sync:
    runs-
# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: ubuntu-latest
    
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        NODE_VERSION: '18'
        PYTHON_VERSION: '3.11'

    steps:
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm
            node_modules
            */*/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          ref: master
      - name: Merge master into beta
        run: |
          git fetch origin beta
          git checkout beta
          git merge master --no-edit || true
          git push origin beta

