# DISABLED v8.5.0 — Redundant, unified-ci.yml covers all syntax purity checks
name: JS Syntax Purity Gate
if: false
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
defaults:
  run:
    shell: bash

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DISABLED — merged into unified-ci.yml validate job"