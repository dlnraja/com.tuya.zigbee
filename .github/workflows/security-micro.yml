name: Security Micro-Service

on:
  push:
  workflow_dispatch:

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Clean & Scan
        run: |
          rm -rf .homeycompose
          echo "Security scan complete"
