# bench-ia.yml
# Description: Workflow automatis̩ pour bench ia
# Date: 2025-07-25
# Version: 1.0

name: Benchmark IA Mensuel


# Permissions pour la s̩curit̩
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on:
  schedule:
    - cr
# Permissions pour la s̩curit̩
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: '15 4 1 * *'

jobs:
  bench-ia:
    runs-
# Permissions pour la s̩curit̩
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: ubuntu-latest
    
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        NODE_VERSION: '18'
        PYTHON_VERSION: '3.11'

    steps:
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm
            node_modules
            */*/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - uses: actions/checkout@v4
      - name: Lancer le benchmark IA
        run: node tools/benchmark-ia.js
      - name: Commit r̩sultats
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "github-actions"
          git add ai-benchmark/
          git commit -m "chore(ia): maj benchmark IA mensuel [bot]" || echo "Nothing to commit"
          git push

