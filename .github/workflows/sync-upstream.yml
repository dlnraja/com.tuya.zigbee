# sync-upstream.yml
# Description: Workflow automatisÃ© pour sync upstream
# Date: 2025-07-25
# Version: 1.0

name: Sync from upstream


# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on:
  schedule:
    - cr
# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: '0 0 * * 0'  # Tous les dimanches à minuit
  workflow_dispatch:

jobs:
  sync:
    runs-
# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: ubuntu-latest
    
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        NODE_VERSION: '18'
        PYTHON_VERSION: '3.11'

    steps:
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm
            node_modules
            */*/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-versi
# Permissions pour la sÃ©curitÃ©
permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read
  security-events: read

on: 18.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Configure Git
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
      
      - name: Sync from upstream
        run: |
          git remote add upstream https://github.com/JohanBendz/com.tuya.zigbee.git
          git fetch upstream
          git checkout -b sync-upstream-$(date +%Y%m%d)
          git merge upstream/master --no-edit
      
      - name: Run driver sync
        run: |
          npm run sync-drivers
          npm run build-readme
      
      - name: Run tests
        run: npm test
      
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          title: 'chore: weekly sync from upstream'
          commit-message: 'chore: sync drivers and update README'
          branch: sync-upstream-$(date +%Y%m%d)
          delete-branch: true
          base: master

