# MEGA ULTIMATE ENHANCED - 2025-08-07T16:33:44.931Z
# Workflow amélioré avec liens corrigés et fonctionnalités étendues

name: 📊 Generate Driver Matrix

on:
  push:
    branches:
      - master
    paths:
      - 'drivers/**'
      - 'generate-matrix.js'
  pull_request:
    paths:
      - 'drivers/**'
  workflow_dispatch:

jobs:
  generate-matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Generate driver matrix
        run: |
          node generate-matrix.js
          echo "📊 Driver matrix generated successfully"

      - name: Upload matrix artifacts
        uses: actions/upload-artifact@v3
        with:
          name: driver-matrix
          path: |
            drivers-matrix.md
            docs/dashboard.html

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add drivers-matrix.md docs/dashboard.html
          git commit -m "📊 docs: update driver matrix and dashboard" || echo "No changes to commit"
          git push

      - name: Create matrix notification
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '📊 Driver Matrix Updated',
              body: `✅ Matrice des drivers mise à jour automatiquement!
              
              📄 **Fichiers générés:**
              - drivers-matrix.md
              - docs/dashboard.html
              
              📅 **Date**: ${new Date().toISOString()}
              📊 **Statut**: Génération automatique
              
              🎯 **MEGA-PROMPT ULTIME - VERSION FINALE 2025**`
            }) 