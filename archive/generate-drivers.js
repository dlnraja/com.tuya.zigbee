const fs = require("fs"); console.log("🚀 GÉNÉRATION DES DRIVERS FAMILLE..."); const drivers = ["plug-tuya-universal", "climate-trv-tuya", "cover-curtain-tuya", "remote-scene-tuya"]; drivers.forEach(driver => { const dir = `drivers/${driver}`; if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true }); console.log(`✅ ${driver} créé`); }); console.log("🎉 GÉNÉRATION TERMINÉE");
